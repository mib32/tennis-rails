class Tennis.Views.CheckoutView extends Backbone.View
  events: {}

  # initialize: ->
  #   $ =>
  #     window.grid.scheduler().bind('save', @render, this)

  # render: ->
  #   $.get('/orders/total.js')
  #   