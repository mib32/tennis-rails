<% breadcrumb :stadiums %>

<div class="body-bg">
	<div class="row">
		<div class="entry">
			<h1><span class="title-tire">–</span> Стадионы <span class="title-tire">–</span></h1>
			<hr>
			<ul>
			<% @stadiums.each do |stadium| %>
				<li>
		  		<%= image_tag nil, class: 'avatar photo pull-left', size: '100x100' %>
					<%= link_to stadium.name, stadium %>
				</li>
			<% end %>
			</ul>
		</div>
	</div>
 </div>
