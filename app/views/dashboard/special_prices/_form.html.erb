  <%= simple_form_for([:dashboard, @court, @special_price]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :start %>
    <%= f.input :end %>
    <%= f.input :price %>
    <%= f.input :is_sale %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
