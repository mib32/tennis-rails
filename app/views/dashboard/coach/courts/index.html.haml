= simple_form_for CoachesCourt.new, url: dashboard_courts_path, wrapper: :inline_form, html: { class: 'form-inline' } do |f|
  = f.association :court, label: 'Корт', collection: Court.all - @coach.courts, label_method: :name_with_stadium
  = f.input :price, label: 'Стоимость часа', as: :decimal
  = f.button :submit, "Привязаться к стадиону"

%h3 Ваши стадионы
%table.table.table-hover
  %thead
    %tr
      %th Название
      %th Корт
      %th Стоимость часа
      %th
  %tbody
    = render collection: @courts, partial: 'court', as: 'court'