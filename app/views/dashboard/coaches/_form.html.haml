= simple_form_for [:dashboard, :stadium, @coach] do |f|
  = f.input :name
  = f.association :courts, as: :check_boxes
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.button :submit, 'Сохранить'