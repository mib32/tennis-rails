= simple_form_for([:admin, @user], html: { class: 'user'} ) do |f|

  .form-inputs
    = f.input :email
    = f.input :name
    = f.input :type, collection: [%w(StadiumUser Стадион), %w(Coach Тренер), %w(Customer Клиент), %w(Admin Админ)]
    = f.input :password
    = f.input :password_confirmation

  .form-actions
    = f.button :submit, 'Сохранить'