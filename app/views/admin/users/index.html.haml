%h1 Пользователи

= link_to 'Создать пользователя', new_admin_user_path, class: 'btn btn-primary'

%table.table.table-stripped
  %tr
    %th Email
    %th Name
    %th Тип
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.name
      %td= t(user.type.underscore) if user.type? 
      %td= link_to 'Редактировать', edit_admin_user_path(user), class: 'btn btn-default btn-xs'
      %td= link_to 'Удалить', [:admin, user], :method => :delete, :data => { :confirm => 'Вы уверены?' }, class: 'btn btn-danger btn-xs'


