= simple_form_for([:admin, @category], html: { class: 'category'} ) do |f|

  .form-inputs
    = f.input :name, label: 'Название'
    = f.input :parent_id, collection: Category.all, as: :select, label: 'Под категорией'

  .form-actions
    = f.button :submit, 'Сохранить'