%h1 Категории

= link_to 'Создать категорию', new_admin_category_path, class: 'btn btn-primary'

%table.table.table-stripped
  %tr
    %th Имя
    %th Под категорией
    %th
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.name
      %td= category.parent.try(:name)
      %td= link_to 'Редактировать', edit_admin_category_path(category), class: 'btn btn-default btn-xs'
      %td= link_to 'Удалить', [:admin, category], :method => :delete, :data => { :confirm => 'Вы уверены?' }, class: 'btn btn-danger btn-xs'


